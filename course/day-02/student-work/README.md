# 學員專案區

請複製 `_template/`，並將副本改名為自己的學號末三碼，例如 `123/`。

也可以直接把 `prompts/student/00-start-dashboard-project.md` 貼給 Codex，讓 Codex 自動建立個人資料夾並下載課堂資料。

請勿把完整學號、姓名或其他個人資料放入專案。`data/raw/` 保存原始資料，不直接修改；整理後的資料放在 `data/processed/`。
