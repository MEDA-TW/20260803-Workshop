# 講師提供的來源紀錄

這四份是本課程預先保留的公開校務原始文件。`crawl_manifest.jsonl` 每行對應一份文件，記錄來源網址、取得時間、原始檔位置與 SHA-256。

請勿修改 `data/raw/` 或此 manifest。你的 Markdown、分析表、chunks、索引與問答紀錄都是衍生結果，應另外寫入既有的 `data/markdown/`、`analysis/`、`data/processed/` 和 `feedback/`。
